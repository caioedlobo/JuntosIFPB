import React from "react";
import { TextField } from "@material-ui/core";

const PasswordText = () => {
  return (
    <TextField
      label="Senha"
      margin="normal"
      fullWidth
      color="primary"
    ></TextField>
  );
};

export default PasswordText;
