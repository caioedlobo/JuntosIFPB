import React from "react";
import { TextField } from "@material-ui/core";
const LoginText = () => {
  return (
    <TextField
      label="Usuário"
      margin="normal"
      color="secondary"
      fullWidth
    ></TextField>
  );
};

export default LoginText;
